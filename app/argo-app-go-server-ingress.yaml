apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argo-app-go-server-ing
  namespace: go-server-ns
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: go.local
    http:
      paths:
      - backend:
          service:
            name: argo-app-go-server
            port:
              number: 8090
        path: /argo-app-go-server
        pathType: Prefix
